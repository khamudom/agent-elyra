import React from 'react';
import ReactDOM from 'react-dom/client';
import ExtensionUI from '../src/views/ExtensionUI';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ExtensionUI />
  </React.StrictMode>,
); 